commands:
  logs:
    description: Get logs of a pod.
    options:
      - name: n
        value: tailLines
    parameters:
      - name: podName
  get:
    description: Get information about objects (pods, services, deployments).
    parameters:
      - name: objectName
